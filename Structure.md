---

### 🎯 核心目标

为桌面宠物项目 **`Pet_Agent`** 集成一个**基于API调用的智能聊天Agent**，而是通过**独立服务**的方式，为其注入理解、记忆和对话能力，让桌宠从“只会动画交互”升级为“能说会道、有个性”的数字生命。

---

### 🧠 核心理念：模块化DAG编排框架

你设计了一个**高度解耦的模块化框架**，核心思想是**“搭积木式构建AI大脑”**：

1. **功能拆分**：将提示词组装、上下文检索、情感分析、用户画像、图像识别、网络搜索、输出标准化等能力拆分为独立的模块。
2. **流程编排**：通过**有向无环图（DAG）**定义模块间的依赖关系和数据流向。系统读取DAG后，按**拓扑序**依次执行模块。
3. **配置驱动**：模块的组装逻辑写在配置文件里，不写死在代码中，实现**热插拔**和**逻辑与代码分离**。

---

### 📁 配置文件设计（“仅节点与边”）

你明确要求配置文件**只包含节点与边的依赖关系**，将“执行逻辑”（如模块参数、条件）分离到另一个配置中。

- **格式**：JSON（适配C++/Qt生态，解析库成熟）。
- **内容**：
  ```json
  {
    "nodes": ["parse_input", "load_persona", "call_llm", ...],
    "edges": [
      { "from": "parse_input", "to": "load_persona" },
      { "from": "load_persona", "to": "call_llm" }
    ]
  }
  ```
- **模块具体配置**（如 `type`、`params`、`parallel_group`、`condition`）放在**另一个独立的JSON文件**中，通过节点ID关联。

---

### 🛠️ 技术实现选型（C++ + Qt）

- **解析与存储**：使用 **Qt的 `QJsonDocument`** 解析JSON，使用**动态邻接表**（`vector<vector<Node>>`）存储图，用入度数组（`indegree`）辅助拓扑排序。
- **拓扑排序**：采用 **Kahn算法**，通过队列逐层剥离入度为0的节点，既能得到执行顺序，又能检测是否存在环。
- **封装**：所有图加载、校验、拓扑排序功能**封装成 `DAGGraph` 类**，对外提供简洁接口（`loadFromJsonFile`、`topologicalSort`、`hasCycle`）。

---